{
  "name": "dead-dialer",
  "version": "1.0.0",
  "description": "Högabo Music - webbaserad CRM-dialer med Twilio Voice SDK v2",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild src/dialer.js --bundle --outfile=public/dialer.js --format=iife --target=es2017",
    "dev": "esbuild src/dialer.js --bundle --outfile=public/dialer.js --format=iife --target=es2017 --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@twilio/voice-sdk": "^2.16.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "twilio": "^5.10.6"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}

